{
  "name": "vue-jobs-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 8000 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}